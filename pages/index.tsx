import type { NextPage } from 'next'
import Head from 'next/head'
import { Button, Footer, GradientText, Header, IconFormatted, LinkFormatted, MappedLink, Typography } from 'origin-storybook'
import requestNavLinks from '../src/components/requestNavLinks'
import styles from '../styles/Home.module.css'

const Home: NextPage<{links:MappedLink<LinkFormatted<IconFormatted>>[]}> = ({
  links
}: {
  links:MappedLink<LinkFormatted<IconFormatted>>[]
}) => {
  return (
    <div className={styles.container}>
      <Head>
        <title>Story.xyz</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header webProperty='story' mappedLinks={links} />
      <main className='max-w-screen-2xl mx-auto px-9'>
        <div className='space-y-10 mb-24'>
          <Typography.H2>
            We build tools that
            <br />
            <GradientText webProperty='story' text='power communities' />
          </Typography.H2>
          <Typography.Body classes='text-2xl'>
            NFT Collections, Community Drops, Artist <br/> Collabs. Made easy.
          </Typography.Body>
          <Button type='primary' webProperty='story' label='Talk to us' size='large' />
        </div>
        <div className='flex items-end'>
          <Typography.H3>
            Collections
          </Typography.H3>
          <Typography.Link classes='ml-6' styles={{
            position: 'relative',
            bottom: '0.4rem'
          }}>
            View all Collections &gt;
          </Typography.Link>
        </div>
      </main>

      <Footer />
    </div>
  )
}

export async function getServerSideProps () {
  const { links } = await requestNavLinks()

  return {
    props: { links }
  }
}


export default Home
